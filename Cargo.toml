[package]
name = "closm-probe"
version = "0.1.0"
edition = "2024"
description = "3D storage visualization tool - Universe metaphor for file systems"
license = "MIT"

[dependencies]
bevy = "0.15"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = "thin"
